using UnityEngine;

public class Rotator2D : MonoBehaviour
{
    //[Header("Rotator Property")]
    //[SerializeField] float RotaionSpeed = 360f;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
